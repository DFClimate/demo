

spring:
  datasource:
    url: jdbc:mysql://172.23.106.60:3333/test?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: root
    initial-size: 3
    min-idle: 1
    max-active: 50
    test-on-borrow: true
    driver-class-name: com.mysql.cj.jdbc.Driver

mybatis:
  mapperLocations: classpath:mapper/*.xml
  typeAliasesPackage: com.example.demo


logging:
  level:
    com.example.demo: debug
server:
  port: 8088
